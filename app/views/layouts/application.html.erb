<!DOCTYPE html>
<html>
<head>
	<title>Sistema de Gestión de Citas Médicas Clínica el Milagro</title>
	<%= css3buttons_stylesheets :include_reset => false %>
	<%= csrf_meta_tag %>
	<%= render 'layouts/stylesheets' %>

</head>
<body>
	<div class="container">
		<%= render 'layouts/header' %>

		<section class="round">
			<%= yield %>
		</section>
	</div>
</body>
</html>